# Core
torch>=2.0
torchvision>=0.15
numpy>=1.24
pyyaml>=6.0

# Backbones & baseline architectures
timm>=0.9.12
segmentation-models-pytorch>=0.3.3

# Dataset — v4 global mine-segmentation (fetch S2 from Planetary Computer)
pystac>=1.10
pystac-client>=0.7
planetary-computer>=1.0
stackstac>=0.5
rioxarray>=0.15
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
pyogrio>=0.7
tqdm>=4.66
Pillow>=9.0

# Dataset — v3 LAMES (legacy / optional)
datasets>=2.16
huggingface-hub>=0.20
gdown>=5.1          # download a public Google Drive folder (private folders: mount in Colab)

# Metrics / efficiency / viz
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
ptflops>=0.7.2

# Optional experiment tracking (used only if installed)
# tensorboard>=2.14
