% Highlights for RSASE submission.
% Elsevier requires 3-5 bullet points, each <= 85 characters (incl. spaces).
% Submit as a separate file named "Highlights". This standalone LaTeX renders them.
\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{enumitem}
\pagestyle{empty}
\begin{document}
\noindent\textbf{Highlights}
\begin{itemize}[leftmargin=1.2em,itemsep=2pt]
  \item SPEAR-Net maps artisanal vs.\ industrial mining in Sentinel-2 at 1.06M parameters
  \item A six-band NDVI/MNDWI/NDTI/BSI prior steers a recall-weighted segmentation decoder
  \item It matches 21--25x larger baselines on mIoU on a global expert-verified test split
  \item The spectral prior recovers small ($<32^2$ px) sites better than larger baselines
  \item Area-stratified recall shows small-site failure is structural, not a capacity limit
\end{itemize}
\end{document}
